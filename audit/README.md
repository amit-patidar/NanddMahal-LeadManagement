# Audit

Store one finding or lesson per topic here. Use `task-history/` for task detail, `production-changes-log.md` for live changes, `prod-log/` for monthly rolled logs, and `screenshots/` for evidence.
