# T-006 QA Feedback

Date: 2026-08-19
Owner: QA review after manager implementation

## Verification passed

- `npm run build` completed successfully.
- Existing SQLite users migrated to salted password hashes; the legacy password column is absent.
- Existing admin and sales credentials authenticate locally without exposing password values.
- An administrator can create a sales user.
- A sales user sees only leads assigned to that account.
- A sales user cannot open another lead by changing the detail URL.
- A sales user cannot access user management.
- An administrator can assign or unassign a lead and the assignment is recorded in the activity timeline.
- Deactivating a user invalidates that user's existing session.
- Logout clears the session and unauthenticated lead requests are rejected.
- The 35-check isolated regression script passes: `node --experimental-sqlite tools/verify-user-access.js`.

## QA feedback

No blocking defect was found in the implemented scope. The Users page, manager/admin assignment controls, and sales assigned-only view are ready for manual browser confirmation after deployment.

Follow-up feedback: mandatory first-login password change is not included yet. Admin password reset is available now; the first-login change requirement should be decided before broader team rollout.

Browser visual verification was not run in this pass because the browser connector was unavailable. The production Render service was not modified.
